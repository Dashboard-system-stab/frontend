

# Подключается к бэкенду по WebSocket: ws://localhost:8765
WEBSOCKET_URL = "ws://localhost:8765"

# Что делает:
# 1. Принимает от пользователя два коэффициента X и Y,
#    отправляет их в формате {"type": "coefficients", "x": 2.0, "y": 2.0}

***
Отправка коэффициентов на бэкенд:
payload = json.dumps({'type': 'coefficients', 'x': x, 'y': y})
return payload, ''
***

#    После этого бэкенд применяет коэффициенты к данным и присылает уже изменённые значения

# 2. Фронденд получает данные {"date": "2024-01-01", "temperature": 20.0}
#    и отрисовывает их на графике в реальном времени

***
Приём данных с бэкенда на фронтенд:
data = json.loads(message['data'])
if 'temperature' in data:
    server_state['dates'].append(data['date'])
    server_state['temps'].append(float(data['temperature']))
***

# Формат входящего сообщения (Бэкенд → Фротенд):
#   {"date": "2024-01-01", "temperature": 20.0}

# Формат исходящего сообщения (Фротенд → Бэкенд):
#   {"type": "coefficients", "x": 2.0, "y": 2.0}


P.S. В файле есть тестовый бекенд, который принимает значения с фротенда и вызвращает результат умножения обратно на фротенд


